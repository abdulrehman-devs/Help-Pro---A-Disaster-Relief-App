// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';


  const navItems = [
    { key: "home", label: "Home", path: role === "victim" ? "/victim/dashboard" : "/donor/dashboard" },
    { key: "requests", label: "Requests History", path: role === "victim" ? "/victim/dashboard/requests" : "/donor/dashboard/requests" },
    { key: "personal", label: "Personal Info", path: role === "victim" ? "/victim/dashboard/profile" : "/donor/dashboard/profile" },
    { key: "feedback", label: "Feedback / Report", path: role === "victim" ? "/victim/dashboard/feedback" : "/donor/dashboard/feedback" },
  ];